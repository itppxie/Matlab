clc, clear all, close all;

% 第一步：先确定坐标轴axis的位置和大小
figure(1);
set(gca, 'Position', [0.1 0.1 0.8 0.8]); % axis位置和大小设置，左边界10%、下边界10%、宽80%、高80%
xlim([0 500]);
ylim([0 1]);

% 第二步：再根据公式确定ground truth线段在figure图板中的起点坐标和终点坐标
% 1、辅助线一，绘制（0.42，0.1）到（0.42，0.9）的虚文本箭头
% hold on;
% annotation('textarrow',[0.42 0.42],... 
%     [0.1 0.9],...                    
%     'String', 'figure中坐标为(0.42, 0.1)',...
%     'Color',[0 0 0],... % 箭头颜色
%     'LineStyle','-.',... 
%     'LineWidth',0.5);

% 2、辅助线二，绘制（0.58，0.1）到（0.58，0.9）的虚文本箭头
% hold on;
% annotation('textarrow',[0.58 0.58],... 
%     [0.1 0.9],...                    
%     'String', 'figure中坐标为(0.58, 0.1)',...
%     'Color',[0 0 0],... % 箭头颜色
%     'LineStyle','-.',... 
%     'LineWidth',0.5);

% 第三步：连接起点坐标和终点坐标，即可得到最终的ground truth线段
% 3、绘制（0.42，0.9）到（0.58，0.9）的实文本箭头，即为真实的ground truth线段
hold on;
annotation('textarrow',[0.42 0.58],... 
    [0.9 0.9],...                    
    'String', 'figure中坐标为(0.42, 0.9)',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   

% 第四步：实际当中，ground truth线段往往要向上平移一点点，方便显示更美观。
% 4、向上平移一点点，绘制（0.42，0.905）到（0.58，0.905）的实线段，即为需要的ground truth线段
hold on;
annotation('textarrow',[0.42 0.58],... 
    [0.905 0.905],...                    
    'String', '(0.42, 0.905)',...
    'Color',[1 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5); 

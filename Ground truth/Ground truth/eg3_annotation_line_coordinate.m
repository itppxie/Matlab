% 线条，显示横线
% 主要是确定线段的两个端点的坐标！

clc, clear, close all;

% 1、绘制（0，0）到（1，1）的实线段
hold on;
annotation('textarrow',[0 1],... % 两个元素的向量[x_begin x_end]，表示箭头的起点和终点的 x 坐标
    [0 1],...                    % 两个元素的向量[y_begin y_end]，表示箭头的起点和终点的 y 坐标
    'String', '文本箭头1',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... % 箭杆样式："-" (默认) | "--" | ":" | "-." | "none"
    'LineWidth',0.5);   % 箭杆宽度

% 2、绘制（0.05，0.02）到（1，0.02）的实线段
hold on;
annotation('textarrow',[0.05 1],... % 两个元素的向量[x_begin x_end]，表示箭头的起点和终点的 x 坐标
    [0.02 0.02],...                    % 两个元素的向量[y_begin y_end]，表示箭头的起点和终点的 y 坐标
    'String', '0.02',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... % 箭杆样式："-" (默认) | "--" | ":" | "-." | "none"
    'LineWidth',0.5);   % 箭杆宽度

% 3、绘制（0.05，0.04）到（1，0.04）的实线段
hold on;
annotation('textarrow',[0.05 1],... % 两个元素的向量[x_begin x_end]，表示箭头的起点和终点的 x 坐标
    [0.04 0.04],...                    % 两个元素的向量[y_begin y_end]，表示箭头的起点和终点的 y 坐标
    'String', '0.04',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... % 箭杆样式："-" (默认) | "--" | ":" | "-." | "none"
    'LineWidth',0.5);   % 箭杆宽度

% 4、绘制（0.05，0.06）到（1，0.06）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.06 0.06],...                    
    'String', '0.06',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   

% 5、绘制（0.05，0.08）到（1，0.08）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.08 0.08],...                    
    'String', '0.08',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   

% 6、绘制（0.05，0.10）到（1，0.10）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.10 0.10],...                    
    'String', '0.10',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 7、绘制（0.05，0.12）到（1，0.12）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.12 0.12],...                    
    'String', '0.12',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 8、绘制（0.05，0.14）到（1，0.14）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.14 0.14],...                    
    'String', '0.14',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 9、绘制（0.05，0.16）到（1，0.16）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.16 0.16],...                    
    'String', '0.16',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 10、绘制（0.05，0.18）到（1，0.18）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.18 0.18],...                    
    'String', '0.18',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 11、绘制（0.05，0.20）到（1，0.20）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.20 0.20],...                    
    'String', '0.20',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 12、绘制（0.05，0.22）到（1，0.22）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.22 0.22],...                    
    'String', '0.22',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 13、绘制（0.05，0.24）到（1，0.24）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.24 0.24],...                    
    'String', '0.24',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 14、绘制（0.05，0.26）到（1，0.26）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.26 0.26],...                    
    'String', '0.26',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 15、绘制（0.05，0.28）到（1，0.28）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.28 0.28],...                    
    'String', '0.28',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 16、绘制（0.05，0.30）到（1，0.30）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.30 0.30],...                    
    'String', '0.30',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 16、绘制（0.05，0.32）到（1，0.32）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.32 0.32],...                    
    'String', '0.32',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 16、绘制（0.05，0.34）到（1，0.34）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.34 0.34],...                    
    'String', '0.34',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 16、绘制（0.05，0.36）到（1，0.36）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.36 0.36],...                    
    'String', '0.36',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 16、绘制（0.05，0.38）到（1，0.38）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.38 0.38],...                    
    'String', '0.38',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 16、绘制（0.05，0.40）到（1，0.40）的实线段
hold on;
annotation('textarrow',[0.05 1],... 
    [0.40 0.40],...                    
    'String', '0.40',...
    'Color',[0 0 0],... % 箭头颜色
    'LineStyle','-',... 
    'LineWidth',0.5);   
% 16、绘制（0.05，0.42）到（1，0.42）的实线段
% 16、绘制（0.05，0.44）到（1，0.44）的实线段
% 16、绘制（0.05，0.46）到（1，0.46）的实线段
% 16、绘制（0.05，0.48）到（1，0.48）的实线段
% 16、绘制（0.05，0.50）到（1，0.50）的实线段
% 16、绘制（0.05，0.52）到（1，0.52）的实线段
% 16、绘制（0.05，0.54）到（1，0.54）的实线段
% 16、绘制（0.05，0.56）到（1，0.56）的实线段
% 16、绘制（0.05，0.58）到（1，0.58）的实线段
% 16、绘制（0.05，0.60）到（1，0.60）的实线段
% 16、绘制（0.05，0.62）到（1，0.62）的实线段
% 16、绘制（0.05，0.64）到（1，0.64）的实线段
% 16、绘制（0.05，0.66）到（1，0.66）的实线段
% 16、绘制（0.05，0.68）到（1，0.68）的实线段
% 16、绘制（0.05，0.70）到（1，0.70）的实线段
% 16、绘制（0.05，0.72）到（1，0.72）的实线段
% 16、绘制（0.05，0.74）到（1，0.74）的实线段
% 16、绘制（0.05，0.76）到（1，0.76）的实线段
% 16、绘制（0.05，0.78）到（1，0.78）的实线段
% 16、绘制（0.05，0.80）到（1，0.80）的实线段
% 16、绘制（0.05，0.82）到（1，0.82）的实线段
% 16、绘制（0.05，0.84）到（1，0.84）的实线段
% 16、绘制（0.05，0.86）到（1，0.86）的实线段
% 16、绘制（0.05，0.88）到（1，0.88）的实线段
% 16、绘制（0.05，0.90）到（1，0.90）的实线段
% 16、绘制（0.05，0.92）到（1，0.92）的实线段
% 16、绘制（0.05，0.94）到（1，0.94）的实线段
% 16、绘制（0.05，0.96）到（1，0.96）的实线段
% 16、绘制（0.05，0.98）到（1，0.98）的实线段
% 16、绘制（0.05，0.99）到（1，0.99）的实线段





























